#include <stdio.h>
int main()
{
	int a=20;
	int b=10;
	
    if (b>a)
    {
        printf("%d geater than %d",b,a);
    }
    if (a>b)
    {
        printf("%d geater than %d",a,b);
    }
    return 0;
}

